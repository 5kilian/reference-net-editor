import { Tool } from '../Tool';

export class TextTool extends Tool {

    constructor (stage) {
        super(stage);
        this.icon = '';
        this.name = 'Text Tool';
    }

    onMouseDown (event) { }

    onMouseMove (event) { }

    onMouseUp (event) { }

    onToolDisable (event) { }

    onToolEnable (event) { }

}