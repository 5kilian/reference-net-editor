import { Connection } from './Connection';
import { Point } from '../util/Point';


export class EllipseConnection extends Connection {

    constructor (x, y) {
        super(x, y);
    }

    position (src, dest, output) {

    }

}
